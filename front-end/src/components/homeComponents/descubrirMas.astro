---
// Define la interface para las props
interface Props {
  class?: string;
  color?: string;
  href?: string;
}

// Destructura las props
const { class: className, color = "text-[#545151]", href = "#" } = Astro.props;
---

<a href={href} class:list={[color, "flex hover:underline ", className]}>
  Descubrir más
  <svg class:list={["w-5 h-5 inline-block ml-2 mt-0.5", className]}>
    <use href="/icons/sprite.svg#flecha-icon"></use>
  </svg>
</a>
